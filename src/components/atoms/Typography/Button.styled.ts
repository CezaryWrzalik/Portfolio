import styled, { css } from "styled-components";

export const TextButton_16 = styled.span(
  ({ theme: { fontSizes, fontFamily } }) => css`
    font-family: ${fontFamily.primary};
    font-size: ${fontSizes[16]}px;
  `
);
